def maquina(limit):
  if(len(limit > 5)):
    return "Error: Too many problems"
    
  first = ""
  second = ""
  third = ""
  forth = ""
  string = ""
  
  for limits in limit:
    limits_String = limits.split
    length = 0

  for max in limits:
    if len(limits_String) > length:
      lenght = len(max)

  if lenght > 4:
    return "Error: Numbers cannot be more than four digits."

  

    if third == "+":
      result = str(a + b)

    elif third == "-":
      result = str(a-b)
    else: 
      return "Error: Operator must be '+' or '-'."

  if not first.isdigit():
    return "Error: Numbers must only contain digits."
  elif not second.isdigit():
    return "Error: Numbers must only contain digits."

  
    

 


  